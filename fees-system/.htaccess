# =====================================================
# Fees Management System v1.0.0 — Local .htaccess
# =====================================================

# -------------------------------
# Error Handling (redirect to global error.php)
# -------------------------------
ErrorDocument 400 /fees-system/error.php?code=400
ErrorDocument 401 /fees-system/error.php?code=401
ErrorDocument 403 /fees-system/error.php?code=403
ErrorDocument 404 /fees-system/error.php?code=404
ErrorDocument 500 /fees-system/error.php?code=500
ErrorDocument 502 /fees-system/error.php?code=502
ErrorDocument 503 /fees-system/error.php?code=503
ErrorDocument 504 /fees-system/error.php?code=504

# -------------------------------
# Basic Security
# -------------------------------
Options -Indexes

<FilesMatch "^(\.env|\.htaccess|\.htpasswd|config\.php|db\.php)$">
  Require all denied
</FilesMatch>

# -------------------------------
# Compression (inherit from global, but safe fallback)
# -------------------------------
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript
</IfModule>

# -------------------------------
# Browser Caching (lighter for FMS)
# -------------------------------
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType text/html "access plus 10 minutes"
  ExpiresByType text/css "access plus 1 week"
  ExpiresByType application/javascript "access plus 1 week"
</IfModule>

# -------------------------------
# Security Headers (inherit + local override)
# -------------------------------
<IfModule mod_headers.c>
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# -------------------------------
# PHP Settings (local override)
# -------------------------------
php_value date.timezone Asia/Kolkata
php_value short_open_tag Off
